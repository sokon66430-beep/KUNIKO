/node_modules
/.next
/out
/build
npm-debug.log*
.DS_Store
*.pem
.env*.local

# Runtime store data — NEVER publish (contains password hashes & live data).
# Keep only the seed master catalog.
/data/stores/
/data/system.json

# Source spreadsheets & scratch files
*.xlsx
*.log
tmp_*.js
